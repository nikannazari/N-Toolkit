"""Streamlit UI for mpv++ (placeholder).

For now, run the CLI:
    python3 mpv++.py

When you start the UI, import the action functions from mpv++.py
(act_random, act_play, act_list, act_add, etc.) and bind them to buttons.
"""


def main():
    print("Streamlit UI not implemented yet. Use mpv++.py for the CLI.")


if __name__ == "__main__":
    main()